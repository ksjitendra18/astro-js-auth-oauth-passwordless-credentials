---
import { EmailVerificationRequestForm } from "../../features/auth/components/email-verification-request-form";
import MainLayout from "../../layout/main-layout.astro";

const isUserLoggedIn = !!Astro.locals.userId;
if (isUserLoggedIn) {
  return Astro.redirect("/dashboard");
}
---

<MainLayout title="Verify Email" description="Verify Email">
  <EmailVerificationRequestForm client:load />
</MainLayout>
